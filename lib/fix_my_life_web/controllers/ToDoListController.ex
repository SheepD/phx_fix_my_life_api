defmodule FixMyLifeWeb.ToDoListController do
  use FixMyLifeWeb, :controller

  def index(_conn, _params) do
  end

  def show(_conn, _params) do
  end

  def update(_conn, _params) do
  end

  def destroy(_conn, _params) do
  end
end
